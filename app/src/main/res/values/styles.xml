<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ReviewRatingBar" parent="Widget.AppCompat.RatingBar.Small">
        <!--
        <item name="android:colorAccent">@color/star_yellow</item>
        <item name="android:colorControlActivated">@color/star_yellow</item>
        <item name="android:colorControlNormal">@color/time_gray</item>
        -->
    </style>
    <style name="mediumRatingBar" parent="Widget.AppCompat.RatingBar">
        <item name="android:colorControlNormal">@color/time_gray</item>
        <item name="android:colorControlActivated">@color/star_yellow</item>
        <item name="android:minHeight">15dp</item>
        <item name="android:maxHeight">15dp</item>
    </style>

</resources>
